from django.urls import path
# from system.views import ProjectList, ProjectManage, ProjectAdd, ProjectAlter, ProjectStarMark

app_name = "system"

urlpatterns = [
    # path("project/", ProjectManage.as_view(), name="project"),
    # path("project/list/", ProjectList.as_view(), name="project_list"),
    # path("project/add/", ProjectAdd.as_view(), name="project_add"),
    # path("project/alter/", ProjectAlter.as_view(), name="project_alter"),
    # path("project/star/", ProjectStarMark.as_view(), name="project_star"),
]
